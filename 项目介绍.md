# 零域（Zero Domain）—— AI 安全网关

<div align="center">

**智能防护层 · 为 AI 编程客户端提供脱敏、审查与审计能力**

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![Tests: 171](https://img.shields.io/badge/Tests-171%20passing-brightgreen)](.)
[![TypeScript](https://img.shields.io/badge/TypeScript-5.8-blue.svg)](https://www.typescriptlang.org/)
[![Bun](https://img.shields.io/badge/Bun-1.0+-orange.svg)](https://bun.sh/)

</div>

---

## 📖 项目概述

**零域（Zero Domain）** 是一个轻量级的 AI 安全网关，专为 Claude Code、Cursor、Codex CLI 等 AI 编程工具设计。它以透明代理的方式工作在客户端与上游 API 之间，提供**敏感信息脱敏**、**操作安全审查**、**完整审计追溯**三大核心能力。

### 设计初衷

最近，模型相关的**信息泄露**屡被曝光（其中不乏大厂案例）；实际使用中也确实遇到过模型**不遵守约束、自行执行高危命令**的情况——包括可能导致删库一类的破坏性操作。与此同时，大量营销内容在鼓吹「用 AI 全自动挖漏洞、一个月挣多少万」：不少人看到「能挣钱」，便以为有了模型和 skill 就等于变强，不问边界、不估后果，先猛干再说，却看不见风险正在逼近。

正是在这样的背景下，**零域诞生**。它不是又一个「把请求转给大模型」的中转站，而是在事故发生前加的一道本机闸门：

1. **流式 / 请求前自动脱敏**  
   在请求发送前对敏感数据做脱敏，降低 AI 自动上传密钥、内网地址、连接串、个人信息等造成的数据泄露。

2. **LLM 双重审查**  
   在请求发送前审查模型准备执行的操作，拦截高危 / 破坏性命令；目标是**事前阻断**，而不是等删库、清数据之后再补救。

3. **AI 请求溯源**  
   记录每一条 AI 请求的放行与拦截；日志可查、可对账，出了问题能立刻定位「放了什么 / 拦了什么」。

### 设计理念

在 AI 辅助编程时代，代码、配置、环境变量会经对话进入远程模型。零域的工程目标是：

- **最小侵入** - 客户端只需更改 Base URL，无需改工作流
- **零配置脱敏** - 自动识别并保护多类敏感信息（密钥、IP、邮箱、连接串等）
- **可审计** - 每个请求有完整记录，支持事后追溯
- **可验证** - 独立审查模型拦截高风险操作，降低误操作概率
- **事前阻断** - 泄露与破坏尽量挡在发出之前，而不是事后补锅

### 典型使用场景

```
┌─────────────────┐     ┌──────────────────────────────────┐     ┌─────────────┐
│  Claude Code    │────▶│  零域代理 (127.0.0.1:8787)       │────▶│  上游 API   │
│  / Cursor       │     │  • JSON-Aware 脱敏 → 占位符      │     │  (Claude /  │
│  / Codex CLI    │     │  • 编码解码 → base64/hex/url     │     │   OpenAI)   │
│                 │     │  • LLM 审查 → 放行/拦截           │     │             │
│                 │◀────│  • 审计日志 → JSONL 持久化        │◀────│             │
└─────────────────┘     │  • 响应还原 → 原文恢复            │     └─────────────┘
                        └──────────────────────────────────┘
```

---

## 🎯 核心功能

### 1. JSON-Aware 智能脱敏（v2.0 新增）

**根本性解决 JSON 结构破坏问题**：传统正则全局替换可能跨越 JSON 边界，导致 830KB 大请求被误杀。新版采用结构化脱敏：

```typescript
// 解析 JSON → 递归遍历对象树 → 只对字符串值脱敏 → 重新序列化
{
  "messages": [{"content": "IP 192.168.1.1"}] 
}
↓ 脱敏
{
  "messages": [{"content": "IP {{IP_abc12345}}"}]  // ✅ 结构完整
}
```

**技术优势**：

- ✅ **永不破坏 JSON**（6 个测试覆盖边界情况）
- ✅ **保持 fail-closed**（无需降级）
- ✅ **键名不脱敏**（避免破坏 API 契约）
- ✅ **性能优化**（跳过短标识符如 `role: "user"`）

**编码绕过防护**（自动解码审查）：

- 审查前自动解码 base64/hex/URL 编码内容
- 支持 2 层嵌套（`btoa(btoa("DROP TABLE"))` 全部解出）
- 解码结果以注解形式追加到审查 prompt

```
Auto-decoded content (系统自动解码结果):
- base64: "RFJPUCBUQUJMRSB1c2Vycw==" → "DROP TABLE users"
- hex: "726D202D7266202F" → "rm -rf /"
```

自动识别并替换 **13 类敏感信息**，上行请求中的敏感数据被替换为语义化占位符（如 `{{IP_abc12345}}`），下行响应时自动还原：

| 类型            | 示例                                                    | 占位符格式                   |
| ------------- | ----------------------------------------------------- | ----------------------- |
| **JWT Token** | `eyJhbGciOi...`                                       | `{{TOKEN_xxxxxxxx}}`    |
| **API Key**   | `sk-ant-abc123...`                                    | `{{APIKEY_xxxxxxxx}}`   |
| **AWS Key**   | `AKIAIOSFODNN7EXAMPLE`                                | `{{AWSKEY_xxxxxxxx}}`   |
| **数据库连接串**    | `mongodb://user:pass@...`                             | `{{CONNSTR_xxxxxxxx}}`  |
| **IP 地址**     | `192.168.1.100`                                       | `{{IP_xxxxxxxx}}`       |
| **邮箱**        | `user@example.com`                                    | `{{EMAIL_xxxxxxxx}}`    |
| **手机号**       | `13812345678`                                         | `{{PHONE_xxxxxxxx}}`    |
| **身份证号**      | `110101199001011234`                                  | `{{IDCARD_xxxxxxxx}}`   |
| **私钥整块**      | `BEGIN … PRIVATE KEY`（含 RSA/EC/DSA/OPENSSH/ENCRYPTED） | `{{PRIVKEY_xxxxxxxx}}`  |
| **用户路径**      | `C:\Users\admin\secret.txt`                           | `{{PATH_xxxxxxxx}}`     |
| **密码字段**      | `"password": "secret123"`                             | `{{PASSWORD_xxxxxxxx}}` |
| **URL 参数**    | `?api_key=abc123`                                     | `{{APIKEY_xxxxxxxx}}`   |

**额外技术亮点**：

- ✅ **SSE 流式边收边还原** - 支持 Server-Sent Events 实时流，占位符跨块边界的正确处理
- ✅ **版本号智能排除** - 不会误将 `Python 3.10.1.1` 识别为 IP 地址
- ✅ **占位符随机化**（v2.0 安全加固）- 改用 crypto 随机值，防止占位符注入攻击
- ✅ **映射上限 + TTL** - 单请求占位符上限、活跃映射淘汰、过期清理
- ✅ **PEM 整块** - 含 `ENCRYPTED` / `DSA` 等 `* PRIVATE KEY`，不只 BEGIN 行

### 2. 本地硬拦 + LLM 双重审查

独立的审查模型（与业务模型分离）对每个请求进行安全评估；**明显破坏面先走本地硬拦**（不依赖 LLM）：

```
请求到达 → 本地硬拦 → ┬─ 命中 → 直接拦截
                      └─ 未命中 → 一审 LLM → ALLOW / BLOCK / REVIEW(二审)
```

**本地硬拦覆盖（节选）**：

- 🚫 Unix：`rm -rf` 系统路径（含 `/tmp` `/var` `/etc` `/home` …）
- 🚫 Windows：`del /f`、`rd /s /q`；PowerShell `Remove-Item -Recurse -Force`、关机/磁盘/下载执行
- 🚫 SQL：`DROP TABLE/DATABASE`、`TRUNCATE`、危险 `DELETE`
- 🚫 绕过手法：base64/hex decoy、零宽/BIDI、西里尔同形字（归一化后再匹配）
- ✅ 安全讨论：邻近「如何/分析/写脚本」可豁免硬拦（仍交 LLM）；「立刻执行」类不豁免
- ✅ 工具参数 / 编码解码结果：永不适用讨论豁免

**配置要点**：

| 配置                       | 含义                             | 推荐     |
| ------------------------ | ------------------------------ | ------ |
| `REVIEW_MODE=llm`        | 开审查（硬拦 + LLM）                  | **默认** |
| `REVIEW_MODE=off`        | **整段审查关闭**（硬拦和 LLM 都不跑；仍脱敏/审计） | 仅临时排障  |
| `REVIEW_FAIL_OPEN=false` | 审查模型故障时拦截                      | **默认** |
| `REVIEW_FAIL_OPEN=true`  | 审查模型故障时放行                      | 不推荐    |

支持提供商：`anthropic` / `openai-chat` / `openai-responses` / `codex`；范围 `all` 或 `api`。

### 3. 完整审计追溯

每个请求生成一条 JSONL 审计记录，包含：

```json
{
  "time": "2026-09-11T21:30:45.123Z",
  "method": "POST",
  "path": "/v1/messages",
  "verdict": "allow",
  "outcome": "success",
  "upstreamStatus": 200,
  "durationMs": 1234,
  "judgeModel": "claude-haiku-4-5-20251001",
  "judgeProvider": "anthropic",
  "reason": "安全的代码查询操作",
  "sanitizedCount": 3
}
```

**审计特性**：

- 📝 **JSONL 格式** - 每行一条记录，易于流式处理和日志聚合
- 🔄 **100MB 自动轮转** - 防止日志文件无限增长
- 🖥️ **终端实时输出** - 可配置只打印拦截记录（`AUDIT_STDOUT=blocked`）
- 🌐 **Web 管理界面** - 赛博朋克风格的可视化仪表板

---

## 💡 技术亮点

### 1. 透传认证模式

启用 `PROXY_AUTH_PASSTHROUGH=true` 后，客户端**无需更换 API Key**，只需修改 Base URL：

```json
{
  "env": {
    "ANTHROPIC_BASE_URL": "http://127.0.0.1:8787"
  }
}
```

原 API Key 原样转发给上游（仍会脱敏/审查/审计），适合：

- 已配置好的 Claude Code / Cursor 环境
- 多用户共享同一代理但使用各自的 Key
- 不想暴露代理层 API Key 的场景

### 2. CCSwitch 动态上游

设置 `UPSTREAM_SOURCE=ccswitch` 后，代理会实时读取 `~/.claude/settings.json` 中的当前 provider：

- ✅ **无需重启** - CCSwitch 切换上游后立即生效
- ✅ **多源支持** - 自动适配不同上游的认证方式（Anthropic / Bearer / OpenAI）
- ✅ **降级保护** - 读取失败时使用 `.env` 中的 fallback 配置

### 3. 客户端设置托管（可选）

启用 `CLAUDE_SETTINGS_AUTO=true` 或 `CODEX_SETTINGS_AUTO=true` 后：

**启动时**：

1. 备份原配置文件的**精确字节**（含注释、格式）
2. 注入代理 Base URL 和临时 API Key
3. 加文件锁防止外部修改冲突

**退出时**（SIGINT / SIGTERM）：

1. 检测配置是否被外部修改
2. 未修改 → 字节级还原备份
3. 已修改 → 保留修改并清理代理配置

**冲突处理**：

- 如果代理运行期间用户手动编辑了配置，退出时保留用户修改
- 如果备份时间戳异常，生成 `.conflict-original` 副本并保护现场

### 4. 依赖极简主义

**运行时依赖**：仅 `smol-toml@1.4.2`（用于解析 Codex 的 TOML 配置）

**核心模块**（TypeScript 源码 + 管理端静态资源 + 测试）：

```
src/
├── index.ts               # 启动入口，信号处理
├── config.ts              # 配置加载与验证
├── proxy.ts               # 核心代理逻辑
├── sanitizer.ts           # 脱敏与还原引擎
├── json-sanitizer.ts      # JSON-Aware 结构化脱敏
├── decoder.ts             # 编码检测与解码
├── judge.ts               # LLM 审查调度器
├── audit.ts               # 审计读写（写队列 + 分页）
├── upstream.ts            # CCS/静态上游解析与短缓存
├── admin.ts               # 管理 API + 静态资源托管
├── admin/static/          # 管理页前端（v2.2 拆分）
│   ├── index.html
│   ├── admin.css
│   └── admin.js
├── claude-settings.ts     # Claude 配置托管
├── codex-settings.ts      # Codex 配置托管
├── config-store.ts        # 持久化配置管理（PUT 合并写入）
└── recover-claude-settings.ts

test/                      # 171 pass / 0 fail
├── security-hardening.test.ts
├── local-hard-block.test.ts
├── optimization-regressions.test.ts
└── ...
```

**无依赖的核心能力**：

- JSON-Aware 脱敏（结构化处理，保证输出合法）
- 编码绕过防护（base64/hex/url；硬拦解码即检）
- 本地硬拦 + LLM 一审/二审
- SSE 流式处理（手写状态机）
- JSONL 审计（读写同队列）
- 管理界面（`src/admin/static/` 静态拆分）

---

## 🔧 解决的实际问题

### 问题 1：敏感信息泄露风险

**场景**：使用 AI 编程助手时，代码中的 API Key、数据库密码会随对话上传到远程模型

**零域方案**：

```typescript
// 原始代码
const apiKey = "sk-ant-abc123456789"
const db = "mongodb://admin:pass@localhost:27017/prod"

// 上传到模型时自动脱敏为
const apiKey = "{{APIKEY_7a43f22b}}"
const db = "{{CONNSTR_9f21d3e4}}"

// 模型返回的代码建议中，占位符自动还原为原文
```

### 问题 2：AI 误操作的不可逆性

**场景**：让 AI 清理临时文件，它执行了 `rm -rf /`

**零域方案**：

- 审查模型识别到 `rm -rf /` 的破坏性 → 返回 `BLOCK`
- 请求被拦截，返回 403：`操作已被安全审查拦截：检测到递归删除根目录`
- 审计日志记录完整上下文，便于事后分析

### 问题 3：审计与合规要求

**场景**：企业使用 AI 工具需满足 SOC2 / ISO27001 审计要求

**零域方案**：

- 所有请求生成 JSONL 审计记录（时间、路径、结果、耗时）
- 支持按路径过滤：`GET /admin/api/audit?path=/v1/messages`
- 拦截记录单独标记，便于安全事件追溯
- 管理后台提供统计分析：放行率、拦截率、平均响应时间

### 问题 4：多环境密钥管理复杂

**场景**：开发/测试/生产环境使用不同的 API Key，频繁切换麻烦

**零域方案**：

- 启用透传模式后，客户端保留原 Key 不变
- 代理根据环境变量或 CCSwitch 自动路由到对应上游
- 单一 Base URL 配置，密钥管理回归客户端本地

---

## 🚀 快速开始

### 安装与配置

```bash
# 1. 克隆项目
git clone <repo-url>
cd zero-domain

# 2. 安装依赖（需要 Bun >= 1.0）
bun install

# 3. 复制配置模板
cp .env.example .env

# 4. 编辑配置（最小化配置）
nano .env
```

**最小化 `.env` 配置**：

```env
# 监听地址
LISTEN_PORT=8787

# 上游配置
UPSTREAM_URL=https://api.anthropic.com
UPSTREAM_API_KEY=sk-ant-your-real-key

# 代理认证（透传模式）
PROXY_AUTH_PASSTHROUGH=true

# 审查配置
REVIEW_MODE=llm
REVIEW_PROVIDER=anthropic
REVIEW_API_KEY=sk-ant-your-review-key
REVIEW_MODEL=claude-haiku-4-5-20251001

# 管理后台令牌
PROXY_ADMIN_TOKEN=change-me-to-secure-token

# 拦截详情需要请求体落盘（blocked=仅拦截，all=全部）
AUDIT_INCLUDE_BODY=blocked
```

### 启动方式

#### Linux / macOS

```bash
bun run start
# 或者 watch 模式
bun run dev
```

#### Windows 一键启动

```cmd
start.bat
```

**start.bat 自动化流程**：

1. 备份 Claude Code 配置
2. 注入代理 Base URL
3. 启动代理服务
4. 打开管理后台浏览器
5. Ctrl+C 退出时自动还原配置

### 配置客户端

#### Claude Code

编辑 `~/.claude/settings.json`：

```json
{
  "env": {
    "ANTHROPIC_BASE_URL": "http://127.0.0.1:8787"
  }
}
```

#### Cursor

Settings → Advanced → Override OpenAI Base URL:

```
http://127.0.0.1:8787
```

#### Codex CLI

编辑 `~/.codex/config.toml`：

```toml
[providers.zero-domain]
api_style = "responses"
base_url = "http://127.0.0.1:8787/codex"
api_key = "proxy-local-token"
```

---

## 🎨 管理后台

访问 `http://127.0.0.1:8787/admin`，在页面输入 `PROXY_ADMIN_TOKEN` 后点击「连接」。  
前端资源：`/admin/static/admin.css`、`/admin/static/admin.js`（源码目录 `src/admin/static/`）。

### 赛博朋克主题界面

- **橙红渐变配色** - 科技感与警示色的平衡
- **扫描线动画** - CRT 屏幕效果
- **响应式布局** - 支持桌面 / 平板 / 手机
- **自动刷新** - 约每 15 秒（页面不可见时跳过）
- **拦截详情标记** - 被拦截内容红色区块 + 相关原文高亮
- **历史加载更多** - 审计日志服务端分页，可续拉全部历史

### 四大视图

#### 1. 仪表盘（Dashboard）

```
┌─────────────┬─────────────┬─────────────┬─────────────┐
│ 总请求数    │ 已放行      │ 已拦截      │ 平均响应    │
│ 1,234       │ 1,189 (96%) │ 45 (4%)     │ 1,234ms     │
└─────────────┴─────────────┴─────────────┴─────────────┘

最近拦截记录（实时表格）
时间          方法   路径            判定   状态  拦截原因
09-11 21:30  POST   /v1/messages   BLOCK  403   检测到 rm -rf
```

统计走全量 `/admin/api/audit/stats`；拦截表另拉 `verdict=block`，避免只看最近一页漏掉历史拦截。

#### 2. 审计日志（Logs）

- 完整请求历史（放行 + 拦截）
- **服务端分页 +「加载更多」**（显示「已加载 N / 共 M 条」；下一页/末页可续拉）
- 点击记录查看详情弹窗
- **拦截详情**（v2.1 / v2.2）
  - 列表默认不带 `requestBody`，点开按 `id` 再拉详情
  - 展示被拦截请求原文（需 `AUDIT_INCLUDE_BODY=blocked` 或 `all`）
  - 红色区块 + `BLOCKED` 徽章；尽量高亮原因/「原文摘录」相关片段
  - 单独展示审查判定行（`BLOCK` / `REVIEW`）
  - 智能预览：优先最近 `messages`，超长约截取 4000 字符（按原因锚点）

#### 3. 数据分析（Stats）

```
放行请求: 1,189 (96.3%)
拦截请求: 45 (3.7%)
跳过审查: 0 (0%)

时段活动统计
时段          请求数  放行  拦截  放行率  平均耗时
最近 5 分钟   12      11    1     91.7%   1,234ms
最近 15 分钟  45      43    2     95.6%   1,156ms
最近 1 小时   234     229   5     97.9%   1,089ms
```

#### 4. 配置管理（Config）

**运行配置**：

- 查看上游（CCS 实时 + 回退）、审查模式、故障策略、超时/并发等
- 审查模式文案：`开启（硬拦+LLM）` / `关闭（整闸跳过）`
- **密钥轮换提醒**：展示审查 Key「已配置 ···末四位」；黄色条列出持久化密钥字段；距上次轮换 ≥30 天会提示更换（**不回传密钥原文**）
- 轮换时间戳写在 `data/zero-domain.config.json.secrets-meta.json`（与配置分文件，不进 env）
- 配置白名单：禁止经管理面改 `LISTEN_HOST` / 各类 `*_PATH`

**.env 在线编辑器**：

- 语法校验：`KEY=VALUE` / `#` 注释 / 空行
- GET 对 KEY/TOKEN 脱敏为 `***`；**禁止把 `***` 原样写回**
- 自动备份 `.env.zero-domain-backup`；原子写入；变更写入审计

---

## 📋 配置参考

### 核心配置项

| 配置项                      | 默认值                              | 说明                                                           |
| ------------------------ | -------------------------------- | ------------------------------------------------------------ |
| `LISTEN_PORT`            | `8787`                           | 代理监听端口                                                       |
| `UPSTREAM_URL`           | -                                | 上游 API 地址（必填）                                                |
| `UPSTREAM_API_KEY`       | -                                | 上游 API Key（必填，透传模式可选）                                        |
| `PROXY_AUTH_PASSTHROUGH` | `false`                          | 是否透传客户端 Key                                                  |
| `REVIEW_MODE`            | `llm`                            | 审查总开关：`llm`=本地硬拦+LLM；`off`=整段审查关闭（仍脱敏/审计）                    |
| `REVIEW_PROVIDER`        | `anthropic`                      | 审查提供商                                                        |
| `REVIEW_MODEL`           | `claude-haiku-4-5-20251001`      | 审查模型                                                         |
| `REVIEW_FAIL_OPEN`       | `false`                          | 审查故障策略：`false`=拦截（推荐）；`true`=放行（仅临时排障）                       |
| `AUDIT_PATH`             | `./data/zero-domain.audit.jsonl` | 审计日志路径                                                       |
| `AUDIT_INCLUDE_BODY`     | `off`                            | 是否落盘请求体：`off` / `blocked` / `all`（看拦截原文请用 `blocked` 或 `all`） |
| `PROXY_ADMIN_TOKEN`      | `admin`                          | 管理后台认证令牌                                                     |

### 高级配置

<details>
<summary><b>CCSwitch 动态上游</b></summary>

```env
UPSTREAM_SOURCE=ccswitch
CCSWITCH_SETTINGS_PATH=~/.claude/settings.json
```

代理会实时读取 CCSwitch 当前 provider，无需重启。

</details>

<details>
<summary><b>审查并发控制</b></summary>

```env
REVIEW_MAX_CONCURRENT=2      # 最大并发审查数
REVIEW_MIN_INTERVAL_MS=100   # 请求间隔（平滑突发）
REVIEW_QUEUE_SIZE=32         # 等待队列长度
REVIEW_QUEUE_BYTES=67108864  # 队列最大字节数（64MB）
```

防止审查模型过载，超出限制的请求会排队或被拒绝。

</details>

<details>
<summary><b>客户端设置托管</b></summary>

```env
CLAUDE_SETTINGS_AUTO=true
CLAUDE_SETTINGS_PATH=~/.claude/settings.json
CLAUDE_PROXY_URL=http://127.0.0.1:8787
CLAUDE_PROXY_API_KEY=proxy-local-token
```

启动时自动注入代理配置，退出时字节级还原。

</details>

完整配置项见 [.env.example](./.env.example)。

---

## 🧪 测试覆盖

当前 **171 pass / 0 fail**：

```bash
bun test
```

### 主要套件

| 测试文件                                                                               | 覆盖范围                             |
| ---------------------------------------------------------------------------------- | -------------------------------- |
| `sanitizer.test.ts` / `json-sanitizer.test.ts`                                     | 脱敏规则、PEM、SSE 还原、JSON-Aware       |
| `local-hard-block.test.ts` / `security-hardening.test.ts`                          | 硬拦、讨论豁免、编码 decoy、密钥轮换提醒、env 脱敏写回 |
| `proxy.test.ts` / `integration.test.ts`                                            | 代理转发、认证、审查、端到端                   |
| `decoder.test.ts` / `judge-verdict.test.ts`                                        | 编码解码、判定解析                        |
| `claude-settings.test.ts` / `codex-settings.test.ts` / `startup-lifecycle.test.ts` | 客户端托管与启停                         |
| `optimization-regressions.test.ts` / `env-file.test.ts`                            | 上游解析、审计 body、env-file API        |

**关键场景**：JWT/API Key/PEM 脱敏；`/tmp` 与 PowerShell 硬拦；BIDI/同形字；ccswitch 禁混绑；配置 GET 不含密钥原文；透传不注入上游 Key。

---

## 🛠️ 常见问题

<details>
<summary><b>Q: 代理会拦截所有请求吗？</b></summary>

**A**: 不会。审查范围可配置：

- `REVIEW_SCOPE=all` - 审查所有路径（推荐）
- `REVIEW_SCOPE=api` - 只审查 `/v1/*` API 路径
- `REVIEW_MODE=off` - **关闭整段审查**（本地硬拦 + LLM 都不跑），仍做脱敏和审计后直接转发
- `REVIEW_FAIL_OPEN` - 与上面无关：管的是审查模型故障时拦还是放（默认 `false`=拦）

管理后台（`/admin`）和健康检查（`/healthz`）始终免审查。

</details>

<details>
<summary><b>Q: 脱敏会影响模型理解吗？</b></summary>

**A**: 不会。占位符是**语义化**的：

- `{{IP_7a43f22b}}` 明确表示这是一个 IP 地址
- `{{APIKEY_9f21d3e4}}` 明确表示这是一个 API Key
- 模型仍能理解上下文，只是看不到真实值

下行响应时，占位符会自动还原为原文。

</details>

<details>
<summary><b>Q: 审查模型会增加多少延迟？</b></summary>

**A**: 典型场景下 **500-2000ms**（取决于审查模型响应速度）：

- Haiku 模型：~500ms
- GPT-4o-mini：~800ms
- Sonnet 模型：~1500ms

可通过 `REVIEW_TIMEOUT_MS` 设置超时（默认 8 秒）。

</details>

<details>
<summary><b>Q: 如何临时禁用审查？</b></summary>

**A**: 三种方式（都会跳过本地硬拦和 LLM；脱敏/审计仍在）：

1. **环境变量**：`REVIEW_MODE=off` 后重启
2. **持久化配置**：编辑 `data/zero-domain.config.json`（优先于 `.env`）后重启
3. **管理后台**：配置页 PUT `REVIEW_MODE=off`（部分项需重启才完全生效）

注意：这与 `REVIEW_FAIL_OPEN` 不同。`FAIL_OPEN=true` 是「审查还开着，但模型挂了就放行」；生产请保持 `false`。

</details>

<details>
<summary><b>Q: 支持哪些审查提供商？</b></summary>

**A**: 当前支持 4 种：

- `anthropic` - Claude 模型（推荐 Haiku）
- `openai-chat` - OpenAI Chat Completions API
- `openai-responses` - OpenAI Responses API
- `codex` - Codex CLI 作为审查引擎

</details>

---

## 📚 进阶文档

| 文档                             | 内容               |
| ------------------------------ | ---------------- |
| [README.md](./README.md)       | 快速开始、最新更新、管理后台用法 |
| [.env.example](./.env.example) | 全量环境变量与注释        |

---

## 🔒 安全性

### 本地优先

- ✅ 脱敏映射仅在内存（单请求上限 + 活跃映射淘汰 + TTL）
- ✅ 审计与持久化配置在 `./data/`（已 `.gitignore`）
- ✅ 默认监听 `127.0.0.1`

### 密钥保护

- ✅ 管理 API 需 `PROXY_ADMIN_TOKEN`（timing-safe；空令牌拒绝）
- ✅ 配置 / env-file GET **不回传**密钥原文；环境编辑禁止写回 `***`
- ✅ 配置页提供**密钥轮换提醒**（持久化字段、末四位、≥30 天建议更换）
- ✅ ccswitch 禁止「外部 URL + 本地 `UPSTREAM_API_KEY`」混绑外带

### 审查绕过与配置风险

- ⚠️ LLM 仍可能被精心构造的 prompt 绕过（模型固有上限）
  - 已加固：本地硬拦（含编码 decoy、同形字/BIDI、PowerShell/`/tmp`）+ LLM 放行后再复核
  - 已加固：收紧 `ALLOW` 解析；工具参数不做讨论豁免
- ⚠️ `REVIEW_MODE=off` 会跳过**整段**审查（硬拦 + LLM）
- ⚠️ `REVIEW_FAIL_OPEN=true` 时审查故障会放行（默认 **false**；启动有告警）

---

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

**开发流程**：

```bash
# 1. Fork 项目并克隆
git clone <your-fork>
cd zero-domain

# 2. 安装依赖
bun install

# 3. 运行测试
bun test

# 4. 启动 watch 模式
bun run dev

# 5. 提交 PR
git checkout -b feature/your-feature
git commit -m "feat: add your feature"
git push origin feature/your-feature
```

**代码规范**：

- TypeScript 严格模式
- 使用 Bun 内置 test runner
- 遵循现有代码风格（函数式 + 类型安全）

---

## 📄 许可证

MIT License - 详见 [LICENSE](./LICENSE)

---

## 🙏 致谢

- **Bun** - 快速的 JavaScript 运行时
- **smol-toml** - 轻量级 TOML 解析器

---

<div align="center">

**零域（Zero Domain）· 让 AI 编程更安全**

[README](./README.md) · [.env.example](./.env.example)

</div>
